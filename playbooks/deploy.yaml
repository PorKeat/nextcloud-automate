---
- name: Deploy Enterprise Cloud Workspace
  hosts: k3s_cluster
  gather_facts: false

  roles:
    # Core Infrastructure
    - nextcloud
    
    # Video Conferencing
    - jitsi
    
    # Document Editors (Uncomment the ONE you want to deploy)
    - collabora
    # - onlyoffice
    # - euro-office
